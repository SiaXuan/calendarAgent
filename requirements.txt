fastapi>=0.111.0
uvicorn[standard]>=0.29.0
anthropic>=0.28.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
httpx>=0.27.0
caldav>=1.3.9
icalendar>=6.0.0
recurring-ical-events>=3.0.0
sse-starlette>=1.6.1

# Multi-format plan import (Phase 4 Step 2). File upload + PDF/DOCX text extraction.
# Images/vision deferred (would add Pillow).
python-multipart>=0.0.9
pypdf>=5.0
python-docx>=1.1

# LangGraph / LangChain stack (Phase A migration)
langgraph>=1.2.0
langchain-anthropic>=1.4.0
langchain-core>=1.4.0
langsmith>=0.8.0

# LangMem long-term memory (Phase C). Pinned to 0.0.x line — pre-1.0.
langmem>=0.0.30,<0.1
